const Spinner = () => {
  return (
    <span
      aria-label="loading"
      className="inline-block w-5 h-5 border-2 border-slate-300 border-t-blue-600 rounded-full animate-spin"
    />
  );
};

export default Spinner;
